version: '3.7'
services:
  daddystudiesdev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - '3000:3000'
    volumes:
      - '.:/app'
      - '/app/node_modules'
    command: 'npm run start'
