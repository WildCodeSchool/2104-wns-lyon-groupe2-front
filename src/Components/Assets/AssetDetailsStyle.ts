import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles((theme) => ({
  folderTitle: { fontSize: '42px' },
  folderSubtitle: { fontSize: '15px' },
}))

export default useStyles
